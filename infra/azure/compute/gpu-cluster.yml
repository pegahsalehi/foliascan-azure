$schema: https://azuremlschemas.azureedge.net/latest/amlCompute.schema.json

name: gpu-fs-dev
type: amlcompute
description: GPU compute cluster for FoliaScan model training.

location: westeurope
size: Standard_NC4as_T4_v3
tier: dedicated

min_instances: 0
max_instances: 1
idle_time_before_scale_down: 120

ssh_public_access_enabled: false

tags:
  project: foliascan
  environment: dev
  workload: gpu
  managed-by: azure-ml-cli